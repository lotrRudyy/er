# Pi runtime environment (copy to local.env on the Pi)

# MQTT broker reachable from the Pi (127.0.0.1 for local mosquitto)
LOCAL_BROKER=127.0.0.1

# Pi user for ownership (used by systemd unit)
PI_USER=rudyy
